import { SortByTimestampPipe } from './sort-by-timestamp-pipe';

describe('SortByTimestampPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
